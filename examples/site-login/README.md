# Site login (optional)

Approve a site login request. The site receives the passport via SSE (`lime-sites-sdk`).

```bash
pip install lime-agents-sdk
export LIME_AGENT_TOKEN=at_...
export LIME_LOGIN_REQUEST_ID=lr_...
python main.py
```
